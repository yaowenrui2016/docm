<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="indi.aby.docm.core.contract.AttachmentMapper">
    <resultMap id="AttachmentEntity" type="indi.aby.docm.core.contract.AttachmentEntity">
        <id column="id" property="id"/>
        <result column="docm_id" property="docmId" />
        <result column="doc_path" property="docPath" />
        <result column="doc_name" property="docName" />
        <result column="state" property="state"/>
        <result column="create_time" property="createTime" typeHandler="indi.rui.common.web.mybatis.DateTypeHandler"/>
        <result column="last_modify_time" property="lastModifyTime" typeHandler="indi.rui.common.web.mybatis.DateTypeHandler"/>
    </resultMap>
    <insert id="add">
        insert into t_attachment(id, docm_id, doc_name, doc_path) values
        <foreach collection="attachments" item="attachment" separator=",">
            (#{attachment.id}, #{attachment.docmId}, #{attachment.docName}, #{attachment.docPath})
        </foreach>
    </insert>
    <update id="update"></update>
    <delete id="delete">
        delete from t_attachment where docm_id = #{id}
    </delete>
    <select id="findTotalNum" resultType="java.lang.Integer"></select>
    <select id="findAll" resultType="indi.aby.docm.core.contract.AttachmentEntity"></select>
    <select id="findById" resultMap="AttachmentEntity">
        select * from t_attachment where docm_id = #{id}
    </select>
    <select id="findByPath" resultMap="AttachmentEntity">
        select * from t_attachment where doc_path = #{docPath} and doc_name = #{docName}
    </select>
</mapper>