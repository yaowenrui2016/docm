<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="indi.aby.docm.core.permission.PermissionMapper">
    <resultMap id="PermissionEntity" type="indi.aby.docm.core.permission.PermissionEntity"
               extends="indi.rui.common.web.dao.IMapper.AbstractData">
        <result column="module" property="module"/>
        <result column="desc" property="desc"/>
    </resultMap>
    <insert id="add">
        insert into t_permission(id, name, module, `desc`) values
        <foreach collection="collection" item="perm" separator=",">
            (#{perm.id}, #{perm.name}, #{perm.module}, #{perm.desc})
        </foreach>
    </insert>
    <delete id="deleteAll">
        delete from t_permission
    </delete>
    <select id="findAll" resultMap="PermissionEntity">
        select * from t_permission limit 0, 1000
    </select>
</mapper>