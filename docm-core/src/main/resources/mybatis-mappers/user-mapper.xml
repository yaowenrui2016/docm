<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="indi.aby.docm.core.entity.UserEntity">

    <insert id="add">
	    INSERT INTO t_account (
	        account_id, username, nickname, email, cellphone, password)
	    VALUES (
	    	#{account_id}, #{username}, #{nickname}, #{email}, #{cellphone}, #{password}
	    )
	</insert>

    <select id="list" parameterType="java.lang.String" resultType="com.rui.study.micro.userself.model.AccountPo">
		SELECT * FROM t_account
		 WHERE account_id = #{identifier} OR username = #{identifier} OR email = #{identifier} OR cellphone = #{identifier}
	</select>

    <select id="get" resultType="com.rui.study.micro.userself.model.AccountPo">
		SELECT * FROM t_account LIMIT 0, 100;
	</select>

	<update id="edit">
		UPDATE
	</update>

    <delete id="delete">
        DELETE FROM t_account
        WHERE account_id = #{identify} OR username = #{identify} OR email = #{identify} OR cellphone = #{identify}
    </delete>
</mapper>